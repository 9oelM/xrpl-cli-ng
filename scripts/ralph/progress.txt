# Ralph Progress Log
Started: Sat Mar  7 08:49:21 AM Universal 2026
---

## 2026-03-07 - US-003
- Implemented E2E network tests for `vault deposit` command
- The vault command implementation was already complete in `src/commands/vault.ts`; validation tests were already in `vault.create.validation.test.ts`
- Created `tests/e2e/vault/vault.deposit.network.test.ts` with 5 tests: XRP deposit, --json, --dry-run, --no-wait, --account/--keystore/--password
- **Learnings for future iterations:**
  - vault.create.validation.test.ts already contains validation tests for ALL vault subcommands (deposit, withdraw, delete, clawback) — only network tests are missing for US-004 through US-006
  - Devnet faucet pattern for vault tests: `fundFromDevnetFaucet(client)` in beforeAll (single call), create vault in beforeAll, each test deposits/withdraws from the same shared vault
  - VaultDeposit accepts amount in drops (string) for XRP; `parseAmount("1")` returns drops as string
---

## 2026-03-07 - US-004
- Created `tests/e2e/vault/vault.withdraw.network.test.ts` with 6 tests: basic XRP withdraw, --destination, --json, --dry-run, --no-wait, --account/--keystore/--password
- vault withdraw implementation was already complete in `src/commands/vault.ts`; validation tests already in `vault.create.validation.test.ts`
- **Learnings for future iterations:**
  - For `--destination` test: fund receiver using `client.submitAndWait` directly in the async `it()` block — no CLI payment command needed
  - Each test deposits 2 XRP then withdraws 1 XRP to keep vault state consistent; tests are sequential so sharing the vault across tests works
  - Devnet faucet provides ~100 XRP; 6 tests × ~2 XRP deposits = ~12 XRP, well within budget
---

## 2026-03-07 - US-005
- Created `tests/e2e/vault/vault.delete.network.test.ts` with 5 tests: create+delete+verify-gone, --json, --dry-run, --no-wait, --account/--keystore/--password
- vault delete implementation was already complete in `src/commands/vault.ts`; validation tests already in `vault.create.validation.test.ts`
- **Learnings for future iterations:**
  - Use `ledger_entry` with `index: vaultId` (direct LedgerIndex lookup) to verify a vault was deleted — NOT `vault: { account, vault_id }` which requires an 'owner' field that the RPC doesn't accept
  - The `ledger_entry` error for a deleted/missing entry is `data.error === "entryNotFound"`
  - For dry-run existence check: same `index: vaultId` pattern works to verify vault still exists
  - Each test creates its own vault (vault delete consumes the vault), so no shared vault across tests
---

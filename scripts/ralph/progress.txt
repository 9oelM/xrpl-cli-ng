## Codebase Patterns
- `resolveWallet` helper is duplicated per command file (not shared util) — copy from credential.ts
- `AuthorizeCredential` from xrpl.js wraps each entry: `{ Credential: { Issuer, CredentialType } }`
- For `deposit-preauth set`, `AuthorizeCredentials`/`UnauthorizeCredentials` each take exactly one entry array
- Enable DepositAuth with `AccountSet` `SetFlag: 9` (asfDepositAuth) in beforeAll for preauth tests
- Credential-based preauth requires funded issuer address on testnet

Started: Fri Mar  6 11:22:04 AM Universal 2026
---

## 2026-03-06 - US-001
- Implemented `deposit-preauth set` subcommand in `src/commands/deposit-preauth.ts`
- Registered command in `src/commands/index.ts` and `src/index.ts`
- Created `tests/e2e/deposit-preauth/deposit-preauth.validation.test.ts` (10 no-network tests)
- Created `tests/e2e/deposit-preauth/deposit-preauth.set.test.ts` (9 funded E2E tests)
- Files changed: src/commands/deposit-preauth.ts, src/commands/index.ts, src/index.ts, tests/e2e/deposit-preauth/deposit-preauth.validation.test.ts, tests/e2e/deposit-preauth/deposit-preauth.set.test.ts
- **Learnings for future iterations:**
  - `AuthorizeCredential` type from xrpl.js: `{ Credential: { Issuer: string; CredentialType: string } }` — the inner `Credential` wrapper is required
  - Set `SetFlag: 9` (asfDepositAuth) on the owner wallet in beforeAll to make preauth semantically meaningful on testnet
  - Must fund credential issuer separately (not just owner+other) for credential-based preauth tests
  - The `--no-wait` test should unauthorize an already-authorized account to avoid duplicate preauth error
---

## 2026-03-06 - US-002
- Implemented `deposit-preauth list <address>` subcommand in `src/commands/deposit-preauth.ts`
- Added pagination support via `account_objects` with `type: 'deposit_preauth'`
- Default output: `Account: <address>` for account-based, `Credential: <issuer> / <decoded-type>` for credential-based
- Uses `convertHexToString` from xrpl.js to decode credential type hex
- Created `tests/e2e/deposit-preauth/deposit-preauth.list.test.ts` (5 tests including --json and removal after unauthorize)
- Files changed: src/commands/deposit-preauth.ts, tests/e2e/deposit-preauth/deposit-preauth.list.test.ts
- **Learnings for future iterations:**
  - `account_objects` with `type: 'deposit_preauth'` returns entries with `Authorize` (account) or `AuthorizeCredentials` (credential) fields
  - Tests must be sequential (authorize → list → unauthorize → list) since they share the same owner account state
  - `convertHexToString` from xrpl.js decodes hex credential type; wrap in try/catch for resilience
---

# Ralph Progress Log
Started: Sat Mar  7 06:07:48 AM Universal 2026
---

## 2026-03-07 - US-005
- Consolidated credential, oracle, ticket, and deposit-preauth tests into single network test files
- credential: merged credential.create/accept/delete/list test files into credential.network.test.ts (42 tickets, 20 test × 2 wallets)
- oracle: merged oracle.set/get/delete test files into oracle.network.test.ts (20 tickets, 18 tests × 1 wallet)
- ticket: merged ticket.create/list test files into ticket.network.test.ts (12 tickets, 10 tests × 1 wallet)
- deposit-preauth: merged deposit-preauth.set/list test files into deposit-preauth.network.test.ts (30 tickets, ~27 wallets)
- All deleted originals; all new files use fundMaster + initTicketPool + createFunded pattern
- Files changed: tests/e2e/credential/credential.network.test.ts (new), tests/e2e/oracle/oracle.network.test.ts (new), tests/e2e/ticket/ticket.network.test.ts (new), tests/e2e/deposit-preauth/deposit-preauth.network.test.ts (new); deleted 11 old test files; scripts/ralph/prd.json (US-001–005 marked passes: true)
- **Learnings for future iterations:**
  - enableDepositAuth helper inside the test file works well for tests that need DepositAuth on individual wallets
  - Credential tests need extra wallet count (2 per test) since both issuer and subject are separate wallets
  - deposit-preauth list tests with 1 wallet still need TICKET_COUNT accounting — make sure budget covers them
---

## 2026-03-07 - US-006
- Consolidated nft and account tests into single network test files
- nft: merged nft.mint/burn/modify into nft.core.test.ts (22 tickets, 18 wallets × 3 XRP, 1 wallet per test)
- nft: merged nft.offer.create/cancel/accept/list into nft.offers.test.ts (35 tickets, 31 wallets × 2 XRP)
- account set: merged account.set.fields/flags/clawback + set.test.ts into account.set.network.test.ts (15 tickets, 12 wallets × 2 XRP); includes mnemonic + keystore tests
- account keys: merged account.set-regular-key + account.delete into account.keys.test.ts (22 tickets, 18 wallets × 2 XRP); merged sequential set/verify and remove/verify tests into single it() blocks
- account query: merged info + balance + transactions + trust-lines + offers + channels + nfts into account.query.test.ts (no faucet needed — all tests use well-known KNOWN_TESTNET_ADDRESS)
- Deleted 20 original test files; all new files use fundMaster + initTicketPool + createFunded pattern
- Files changed: 5 new test files; deleted 20 old files; scripts/ralph/prd.json (US-006 marked passes: true)
- **Learnings for future iterations:**
  - nft.offer accept tests that pay for NFTs need small amounts (0.01 XRP) so buyer can afford the purchase with only 2 XRP funded
  - set.test.ts (the original account set test) was not mentioned in the PRD merge list but had a faucet call — include it in the account.set.network.test.ts merge and delete it
  - Sequential tests that verify on-chain state (set-then-verify) should be merged into a single it() block with sleep() in between; this avoids wallet-sharing across test cases
  - account.query.test.ts needs no faucet call at all — all tests query the testnet genesis address which already has state
---

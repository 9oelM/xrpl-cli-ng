# Ralph Progress Log
Started: Thu Mar  5 00:40:32 Universal 2026
---

## Codebase Patterns
- Tests use `spawnSync(TSX, [CLI, ...args])` with `encoding: "utf-8"` for E2E testing. TSX = `node_modules/.bin/tsx`, CLI = `src/index.ts`
- PATH must be set in env for E2E tests: `/home/vscode/.fnm/node-versions/v22.22.0/installation/bin:${process.env.PATH}`
- Use tmp dirs with `mkdtempSync` + `rmSync(dir, { recursive: true })` in try/finally for test isolation
- getKeystoreDir resolution order: options.keystore → XRPL_KEYSTORE env var → ~/.xrpl/keystore/
- resolveAccount: regex `/^r[a-zA-Z0-9]{24,33}$/` matches XRPL addresses; else scan keystore for label match
- encryptKeystore now takes optional 5th param `label?: string`; included in output only when provided
- All wallet commands import getKeystoreDir from `../../utils/keystore.js` (not local copies)
- Atomic writes use writeFileSync to `<path>.tmp` then renameSync to `<path>` (same pattern for all keystore mutations)
- Alias uniqueness scan: readdirSync + filter `.json` + parse each, skip on error

## 2026-03-05 - US-001
- Implemented: label field on KeystoreFile, getKeystoreDir utility extracted to keystore.ts, resolveAccount() utility, updated all wallet commands
- Files changed: src/utils/keystore.ts, src/utils/keystore.test.ts, src/commands/wallet/list.ts, src/commands/wallet/remove.ts, src/commands/wallet/import.ts, src/commands/wallet/decrypt-keystore.ts, src/commands/wallet/change-password.ts
- **Learnings for future iterations:**
  - Codebase uses ESM modules (package.json "type": "module"), imports need .js extension
  - Edit tool requires file to be read before editing (even if read earlier in conversation)
  - All 97 existing tests pass after refactor
---

## 2026-03-05 - US-002
- Implemented: `wallet alias set <address> <name>` and `wallet alias remove <address>` subcommands
- Files changed: src/commands/wallet/alias.ts (new), src/commands/wallet/index.ts, tests/e2e/wallet/alias.test.ts (new)
- **Learnings for future iterations:**
  - Alias command is a subcommand group: `new Command("alias")` with `.command("set")` and `.command("remove")` chained on it
  - Uniqueness check: scan all *.json in keystoreDir, skip file for the target address itself
  - aliasCommand registered in wallet/index.ts with `walletCommand.addCommand(aliasCommand)`
  - All 102 tests pass
---

## 2026-03-05 - US-004
- Implemented: `--alias <name>` option on `wallet import`; stores label in keystore file at import time, applies alias uniqueness check (same logic as `alias set`), updated confirmation message
- Files changed: src/commands/wallet/import.ts, tests/e2e/wallet/import.test.ts
- **Learnings for future iterations:**
  - `checkAliasUniqueness` logic was duplicated from alias.ts into import.ts (not exported from alias.ts); consider extracting to keystore utils if needed again
  - Alias uniqueness check happens before `encryptKeystore` call so we exit 1 before writing any file
  - All 107 tests pass
---

## 2026-03-05 - US-005
- Implemented: `wallet list` now reads label from each keystore JSON file and shows alias column
- Human-readable output: `<address>  <alias>` when alias set, just `<address>` when not
- `--json` output changed from `string[]` to `{ address: string, alias?: string }[]`; alias key omitted when not set
- Files changed: src/commands/wallet/list.ts, tests/e2e/wallet/list.test.ts
- **Learnings for future iterations:**
  - Existing tests that parsed `--json` as `string[]` needed updating to parse as `{ address: string, alias?: string }[]`
  - Human-readable output: only append alias when defined, no dash placeholder for absent aliases
  - All 109 tests pass
---

## 2026-03-05 - US-003
- Implemented: `wallet alias list` subcommand added to the alias group in alias.ts
- Files changed: src/commands/wallet/alias.ts, tests/e2e/wallet/alias.test.ts
- **Learnings for future iterations:**
  - `list` scans readdirSync + filters .json + collects entries where data.label && data.address
  - `--json` flag outputs JSON.stringify(aliases) array; human output is `alias  →  address` per line
  - Empty case: catch on readdirSync (dir may not exist) returns [] aliases → prints "(no aliases set)"
  - All 8 alias tests pass (including 3 new list tests)
---

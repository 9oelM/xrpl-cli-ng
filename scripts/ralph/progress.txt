## Codebase Patterns
- `resolveWallet()` helper in escrow.ts is the canonical pattern for key material resolution — replicate it in new commands
- `--no-wait` option: NEVER pass `false` as default; use `.option("--no-wait", ...)` with no third arg; property is `options.wait` (bool); check `!options.wait`
- Credential ledger entry IDs: extract via `isCreatedNode(n) && n.CreatedNode.LedgerEntryType === "Credential"` then `n.CreatedNode.LedgerIndex`
- `convertStringToHex` from xrpl.js converts UTF-8 strings to hex for CredentialType / URI fields
- `isoTimeToRippleTime` from xrpl.js converts ISO8601 to XRPL epoch for Expiration fields
- CredentialCreate type is available in xrpl.js v4; Subject, CredentialType (hex), URI (hex), Expiration (XRPL epoch) are the fields
---

# Ralph Progress Log
Started: Fri Mar  6 10:24:14 AM Universal 2026
---

## 2026-03-06 - US-002
- Implemented `xrpl credential accept` subcommand
- Files changed: src/commands/credential.ts (added CredentialAcceptOptions interface + credentialAcceptCommand + registered it), tests/e2e/credential/credential.validation.test.ts (added 4 accept validation tests), tests/e2e/credential/credential.accept.test.ts (new, 5 E2E tests)
- **Learnings for future iterations:**
  - CredentialAccept type is exported from xrpl.js v4; fields: Account (subject/sender), Issuer (required), CredentialType (hex)
  - Verified on-chain acceptance via account_objects RPC with type: "credential" — lsfAccepted flag is 0x00010000 (65536)
  - Tests fund subject from issuer (not faucet) to avoid rate limits; use distinct credential type strings per test to avoid tecDUPLICATE
  - All output modes (--json, --dry-run, --no-wait) and --credential-type-hex variant are fully covered
---

## 2026-03-06 - US-001
- Implemented `xrpl credential create` subcommand
- Files changed: src/commands/credential.ts (new), src/commands/index.ts, src/index.ts, tests/e2e/credential/credential.validation.test.ts (new), tests/e2e/credential/credential.create.test.ts (new)
- **Learnings for future iterations:**
  - CredentialCreate type exists in xrpl.js v4 at `xrpl/dist/npm/models/transactions/CredentialCreate.d.ts`
  - `convertStringToHex` and `isoTimeToRippleTime` are importable from `xrpl` — use them for encoding
  - Credential ledger entry type is `"Credential"` (singular) in AffectedNodes
  - The `--no-wait` Commander gotcha: use `options.wait` (not `options.noWait`), no default arg
  - Tests fund subject wallet from issuer (not faucet) to minimize faucet calls
---

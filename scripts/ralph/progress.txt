## Codebase Patterns
- Shared CLI runner: `tests/helpers/cli.ts` exports `runCLI(args, extraEnv?)` — all E2E tests import from here
- Runtime PATH resolution: `path.dirname(process.execPath) + path.delimiter + process.env.PATH` works in fnm (local) and setup-node (CI)
- Some test files use `resolve` from `path` for `mkdtempSync(resolve(tmpdir(), ...))` — keep that import even after removing CLI/TSX/E2E_PATH boilerplate

# Ralph Progress Log
Started: Fri Mar  6 05:45:09 AM Universal 2026
---

## 2026-03-06 - US-001
- Created `tests/helpers/cli.ts` with shared `runCLI(args, extraEnv?)` using runtime PATH from `path.dirname(process.execPath)`
- Updated all 35 E2E test files to import `runCLI` from the shared helper, removing per-file boilerplate
- Fixed `account/set.test.ts` which used `exec`/`execAsync` with inline TSX/CLI/E2E_PATH — converted to `runCLI`
- Fixed `wallet/import.test.ts` which had an inline `spawnSync` call — converted to `runCLI` with extraEnv
- **Learnings for future iterations:**
  - Some files use `resolve` from `path` for `mkdtempSync` — must keep that import even when removing CLI runner boilerplate
  - `account/set.test.ts` had an unusual `exec`+`promisify` pattern in `beforeAll`; converted to synchronous `runCLI`
  - Typecheck passes with no issues after the refactor
---

## 2026-03-06 - US-002
- Created `.github/workflows/typecheck.yml` triggering on push and pull_request
- Workflow named 'Typecheck', job named 'typecheck', uses actions/checkout@v4 + actions/setup-node@v4 (node 22, npm cache)
- Runs `npm ci` then `npm run typecheck`
- **Learnings for future iterations:**
  - GitHub Actions workflows go in `.github/workflows/` — directory must be created if it doesn't exist
  - `on: [push, pull_request]` covers all branches by default
---

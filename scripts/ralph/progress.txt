# Ralph Progress Log
Started: Fri Mar  6 04:35:57 PM Universal 2026
---

## Codebase Patterns
- PermissionedDomains amendment IS enabled on testnet (verified via `feature` API call)
- `AuthorizeCredential` type: `{ Credential: { Issuer: string, CredentialType: string } }` — CredentialType must be hex
- `PermissionedDomainSet` (no DomainID = create), `PermissionedDomainDelete` (requires DomainID) are both in xrpl.js
- Use `isCreatedNode(n) && n.CreatedNode.LedgerEntryType === 'PermissionedDomain'` to extract domain ID from tx metadata
- The `src/index.ts` file cannot be edited with the Edit tool if not previously Read; use `sed` as fallback
- `isValidClassicAddress` is exported from xrpl and can validate issuer addresses

## 2026-03-06 - US-001
- Implemented `src/commands/permissioned-domain.ts` with `create` subcommand
- Registered `permissionedDomainCommand` in `src/commands/index.ts` and `src/index.ts`
- Created validation tests (6 tests, no network) in `tests/e2e/permissioned-domain/permissioned-domain.validation.test.ts`
- Created network tests (7 tests, testnet) in `tests/e2e/permissioned-domain/permissioned-domain.create.test.ts`
- Files changed: src/commands/permissioned-domain.ts, src/commands/index.ts, src/index.ts, 2 test files
- **Learnings for future iterations:**
  - PermissionedDomains is live on testnet — no need for devnet
  - `--credential <issuer>:<type>` splits on first `:`, type is UTF-8 auto-encoded to hex
  - `--credentials-json` takes pre-hex `credential_type` in each item
  - JSON output format is `{result, domainId, tx}` per PRD spec (simpler than other commands)
  - Domain ID is extracted from `CreatedNode.LedgerIndex` where `LedgerEntryType === 'PermissionedDomain'`
  - For US-002 (update): same file, add `update` subcommand with `--domain-id` required option
  - For US-003 (delete): add `delete` subcommand using `PermissionedDomainDelete` tx type
---

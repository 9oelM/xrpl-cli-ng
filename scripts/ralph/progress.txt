## Codebase Patterns
- `resolveWallet` helper is duplicated per command file (not shared util) — copy from credential.ts
- `AuthorizeCredential` from xrpl.js wraps each entry: `{ Credential: { Issuer, CredentialType } }`
- For `deposit-preauth set`, `AuthorizeCredentials`/`UnauthorizeCredentials` each take exactly one entry array
- Enable DepositAuth with `AccountSet` `SetFlag: 9` (asfDepositAuth) in beforeAll for preauth tests
- Credential-based preauth requires funded issuer address on testnet

Started: Fri Mar  6 11:22:04 AM Universal 2026
---

## 2026-03-06 - US-001
- Implemented `deposit-preauth set` subcommand in `src/commands/deposit-preauth.ts`
- Registered command in `src/commands/index.ts` and `src/index.ts`
- Created `tests/e2e/deposit-preauth/deposit-preauth.validation.test.ts` (10 no-network tests)
- Created `tests/e2e/deposit-preauth/deposit-preauth.set.test.ts` (9 funded E2E tests)
- Files changed: src/commands/deposit-preauth.ts, src/commands/index.ts, src/index.ts, tests/e2e/deposit-preauth/deposit-preauth.validation.test.ts, tests/e2e/deposit-preauth/deposit-preauth.set.test.ts
- **Learnings for future iterations:**
  - `AuthorizeCredential` type from xrpl.js: `{ Credential: { Issuer: string; CredentialType: string } }` — the inner `Credential` wrapper is required
  - Set `SetFlag: 9` (asfDepositAuth) on the owner wallet in beforeAll to make preauth semantically meaningful on testnet
  - Must fund credential issuer separately (not just owner+other) for credential-based preauth tests
  - The `--no-wait` test should unauthorize an already-authorized account to avoid duplicate preauth error
---

## 2026-03-06 - US-002
- Implemented `deposit-preauth list <address>` subcommand in `src/commands/deposit-preauth.ts`
- Added pagination support via `account_objects` with `type: 'deposit_preauth'`
- Default output: `Account: <address>` for account-based, `Credential: <issuer> / <decoded-type>` for credential-based
- Uses `convertHexToString` from xrpl.js to decode credential type hex
- Created `tests/e2e/deposit-preauth/deposit-preauth.list.test.ts` (5 tests including --json and removal after unauthorize)
- Files changed: src/commands/deposit-preauth.ts, tests/e2e/deposit-preauth/deposit-preauth.list.test.ts
- **Learnings for future iterations:**
  - `account_objects` with `type: 'deposit_preauth'` returns entries with `Authorize` (account) or `AuthorizeCredentials` (credential) fields
  - Tests must be sequential (authorize → list → unauthorize → list) since they share the same owner account state
  - `convertHexToString` from xrpl.js decodes hex credential type; wrap in try/catch for resilience
---

## 2026-03-07 - US-003
- Consolidated channel tests: channel.create + channel.fund + channel.list + channel.claim → channel.network.test.ts (22 tests, 1 faucet call)
- Consolidated escrow tests: escrow.create + escrow.finish + escrow.cancel + escrow.list → escrow.network.test.ts (23 tests, 1 faucet call)
- Updated tests/e2e/helpers/fund.ts: added TransactionResult check in createFunded/fundAddress to surface tecUNFUNDED_PAYMENT errors clearly
- Files changed: tests/e2e/channel/channel.network.test.ts (new), tests/e2e/escrow/escrow.network.test.ts (new), tests/e2e/helpers/fund.ts, deleted 8 original test files
- **Learnings for future iterations:**
  - XRPL testnet faucet gives ~100 XRP (NOT 1000). Budget formula: available = 100 - 1(base_reserve) - TICKET_COUNT×0.2(owner_reserve). Net cost per wallet = amountXrp - 0.2 (ticket reserve freed on use)
  - Testnet base_reserve = 1 XRP, owner_reserve = 0.2 XRP per object (confirmed via server_info). Old notes saying 10 XRP base were wrong.
  - For channel tests with 44 wallets: use FUND_AMOUNT=2 XRP and TICKET_COUNT=48. Channel amount=0.5 XRP, fund amount=0.2 XRP fits in 2 XRP wallet (1 base + 1 available).
  - For escrow tests: shared sender/receiver wallets (TICKET_COUNT=5). Fund sender with 30 XRP — supports 10 pre-created escrows (2 XRP×10 locks + 0.2×10 reserves = 22 XRP) plus test-body creates.
  - Always add result check after submitAndWait in funding helpers — otherwise tecUNFUNDED_PAYMENT causes confusing "account not found" timeout instead of clear error message.
---

## 2026-03-07 - US-004
- Consolidated check tests: check.create + check.cash + check.cancel + check.list → check.network.test.ts (20 tests, 1 faucet call)
- Consolidated clawback tests: clawback.iou + clawback.mpt → clawback.network.test.ts (7 tests, 1 faucet call)
- Consolidated multisig tests: multisig.set + multisig.delete + multisig.list → multisig.network.test.ts (12 tests, 1 faucet call)
- Deleted 9 original test files
- Files changed: tests/e2e/check/check.network.test.ts (new), tests/e2e/clawback/clawback.network.test.ts (new), tests/e2e/multisig/multisig.network.test.ts (new), deleted 9 originals
- **Learnings for future iterations:**
  - CheckCreate requires destination to be an activated (funded) account — `tecNO_DST` if using `Wallet.generate()` as destination in submit tests. Dry-run tests are fine with any address.
  - For clawback setup inside each test, run 3-4 direct xrpl.js calls (AccountSet, TrustSet, Payment) before the CLI command — this keeps each test self-contained and avoids shared state.
  - Multisig signer wallets don't need to be funded — XRPL only requires valid addresses in the SignerList. Use `Wallet.generate()` for signer addresses.
  - `SignerList` counts as ONE owner object regardless of signer count — 0.2 XRP reserve for any size signer list.
  - For clawback MPT dry-run: dummy MPT issuance ID must be 48 hex chars (4 bytes seq + 20 bytes account) — use `"00000001" + "AABBCCDD".repeat(5)`.
---

# Ralph Progress Log
Started: Sat Mar  7 04:22:54 AM Universal 2026
---

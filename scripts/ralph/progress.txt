## Codebase Patterns
- PaymentChannel ledger entry type in AffectedNodes metadata is "PayChannel" NOT "PaymentChannel"
- xrpl.js `isCreatedNode(n)` helper from "xrpl" is the correct way to narrow Node union type
- Use `TransactionMetadataBase` (not `TransactionMetadata`) for generic channel/offer transactions
- resolveWallet pattern is duplicated per command file (no shared util yet) — follow the same shape
- XRPL epoch conversion: Math.floor(new Date(iso).getTime()/1000) - 946684800
---

# Ralph Progress Log
Started: Fri Mar  6 07:58:31 AM Universal 2026
---

## 2026-03-06 - US-001
- Implemented `xrpl channel create` subcommand in `src/commands/channel.ts`
- Registered `channelCommand` in `src/commands/index.ts` and `src/index.ts`
- Created `tests/e2e/channel/channel.validation.test.ts` (6 no-network tests, all pass)
- Created `tests/e2e/channel/channel.create.test.ts` (7 testnet tests, all pass)
- Files changed: src/commands/channel.ts, src/commands/index.ts, src/index.ts, tests/e2e/channel/channel.validation.test.ts, tests/e2e/channel/channel.create.test.ts
- **Learnings for future iterations:**
  - CRITICAL: The XRPL ledger entry type for a payment channel in `AffectedNodes` metadata is `"PayChannel"`, NOT `"PaymentChannel"`. Using the wrong string causes channelId extraction to silently return null.
  - Use xrpl.js `isCreatedNode(n)` (imported from "xrpl") to properly narrow the Node discriminated union — do not try to access `n.CreatedNode?.prop` directly
  - `TransactionMetadataBase` is the correct type to import for generic tx metadata (not `TransactionMetadata` which is a complex conditional type)
  - The `--no-wait` option follows the memory pattern: no default arg, `options.wait` is the property name, `if (!options.wait)` is the check
---

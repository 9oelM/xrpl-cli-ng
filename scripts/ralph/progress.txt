# Ralph Progress Log
Started: Sat Mar  7 10:31:40 AM Universal 2026
---

## Codebase Patterns
- Vault commands use devnet (`wss://s.devnet.rippletest.net:51233`), not testnet — SingleAssetVault amendment not on testnet
- Devnet faucet helper: `tests/helpers/devnet.ts` → `fundFromDevnetFaucet(client)` returns a funded wallet
- Vault network tests use sequential `it()` (not `it.concurrent`) with a shared issuer/vault in `beforeAll` and distinct holders for each test case
- VaultClawback `Amount` field: devnet requires it even though xrpl.js types mark it optional — always specify Amount; omitting causes `tecWRONG_ASSET`
- Vault share token ownership is held via MPT in the holder's account objects
- All vault commands are in `src/commands/vault.ts`; validation tests in `vault.create.validation.test.ts`

---

## 2026-03-07 - US-006
- Implemented vault clawback command (`vault clawback --vault-id <hash> --holder <address>`)
- Command already existed in `src/commands/vault.ts` (vaultClawbackCommand) and validation tests in `vault.create.validation.test.ts`
- Created `tests/e2e/vault/vault.clawback.network.test.ts` with 6 devnet tests
- Files changed: `tests/e2e/vault/vault.clawback.network.test.ts`
- **Learnings for future iterations:**
  - VaultClawback requires `Amount` on the XRPL devnet even though the xrpl.js type and docs say it's optional — omitting Amount always returns `tecWRONG_ASSET` on devnet; always specify the amount explicitly
  - VaultClawback only works for IOU/MPT vaults (not XRP); the issuer must have `asfAllowTrustLineClawback` enabled BEFORE any trust lines are created
  - IOU vault test setup: issuer must enable `asfDefaultRipple` + `asfAllowTrustLineClawback`, create vault, fund holders, set up trust lines, issue tokens — all sequentially due to devnet
  - Use `tests/helpers/devnet.ts` `fundFromDevnetFaucet` for a single faucet call; then fund sub-wallets via direct Payment transactions from the issuer
---

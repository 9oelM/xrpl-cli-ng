# Ralph Progress Log
Started: Sat Mar  7 08:28:44 AM Universal 2026
---

## Codebase Patterns
- vitest concurrency is controlled via `poolOptions.forks.maxForks` (file-level) and `maxConcurrency` (test-case level within a file)
- fund.ts fallback pattern: catch err, check `msg.includes("Timeout")`, create fresh Client(XRPL_WS_FALLBACK), connect/use/disconnect in try/finally, rethrow original error if fallback also fails

## 2026-03-07 - US-001
- Added `maxConcurrency: 3` and `poolOptions: { forks: { maxForks: 3 } }` to vitest.config.ts
- Files changed: vitest.config.ts
- **Learnings for future iterations:**
  - `poolOptions.forks.maxForks` caps concurrent worker processes (test files); `maxConcurrency` caps concurrent `it.concurrent` cases within one file
  - Together they create 3×3 = 9 max simultaneous test cases
---

## 2026-03-07 - US-002
- Added `XRPL_WS_FALLBACK` export and `FAUCET_URL_FALLBACK` constant to fund.ts
- Added timeout-based retry logic in `fundMaster`, `initTicketPool`, and `createFunded`
- Fallback client is ephemeral: freshly created, connected before use, disconnected in finally block
- Files changed: tests/e2e/helpers/fund.ts
- **Learnings for future iterations:**
  - Only catch errors where `msg.includes("Timeout")` — do not swallow XRPL transaction errors
  - `Awaited<ReturnType<typeof client.submitAndWait>>` is the correct type for capturing the result before a try/catch split
  - The fallback faucet URL for xrpl-labs testnet is `https://testnet.xrpl-labs.com/accounts`
---

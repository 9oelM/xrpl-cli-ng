# Ralph Progress Log
Started: 2026-03-05

## Codebase Patterns
- E2E tests use `spawnSync` from `child_process` with `tsx` to run the CLI; always pass `PATH` env to ensure `tsx` resolves node correctly
- For non-TTY detection in CLI commands, use `process.stdin.isTTY` (falsy when stdin is piped/redirected)
- CLI commands use `getNodeUrl(cmd)` for node resolution (reads global --node flag via cmd.optsWithGlobals())
- Use `resolveAccount(addressOrAlias, keystoreDir)` with `getKeystoreDir({ keystore: undefined })` for address/alias resolution
- `withClient(url, fn)` wraps connect/disconnect around XRPL requests
- TSX = `node_modules/.bin/tsx`, CLI = `src/index.ts`; PATH = `/home/vscode/.fnm/node-versions/v22.22.0/installation/bin:${process.env.PATH}`
- Use mkdtempSync + rmSync(dir, { recursive: true }) in try/finally for test isolation
- `encryptKeystore` / `decryptKeystore` / `getKeystoreDir` / `resolveAccount` all in `src/utils/keystore.ts`
- `getNodeUrl(cmd)` in `src/utils/node.ts`; `resolveNodeUrl()` in `src/utils/client.ts`
- Atomic writes: writeFileSync to `<path>.tmp` then renameSync to `<path>`
- `process.exit(1)` after `process.stderr.write(msg)` — do NOT throw (commander catches throws)
- In beforeAll with xrpl.js WebSocket + CLI calls: use execAsync (promisify(exec)) NOT spawnSync to avoid blocking the event loop
- After submitting a tx, `await new Promise(res => setTimeout(res, 8000))` is reliable for testnet validation
- `rHb9CJAWyB4rj91VRWn96DkukG4bwdtyTh` is the XRPL testnet genesis address (always funded, good for read-only tests)
- `client.autofill(tx)` handles Sequence, Fee, LastLedgerSequence — always call before signing
- For submitAndWait, catch TimeoutError and exit 1 with 'Error: transaction expired (LastLedgerSequence exceeded)'
- Transaction failures (tec*, tef*, tem*): exit 1, print error code + message to stderr
- Memo text encoding: `Buffer.from(text, 'utf8').toString('hex').toUpperCase()`
- Amount format: plain decimal = XRP; `drops` suffix = raw drops; `value/currency/issuer` = IOU; `value/<48-char-hex>` = MPT
- MPT issuance ID is exactly 48 hex chars — use length check to distinguish from IOU in parser
- `src/utils/amount.ts` is the shared amount parser for all payment-related commands
